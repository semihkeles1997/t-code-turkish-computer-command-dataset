# T-Code v4 Dataset Card

T-Code v4 is a Turkish computer-control command resource designed for token-level decomposition of compound commands into ordered executable subtasks.

## Composition

- Final resource: 2,700 records
- Domains: 4 balanced domains, 675 records each
- Task-token labels: 33 classes including `O`
- Boundary labels: 3 (`B-TASK`, `I-TASK`, `O`)
- Main data format: JSONL

## Domains

- `temel_bilgisayar`
- `tarayıcı_internet`
- `ofis_metin_düzenleme`
- `dosya_yazılım_geliştirme`

## Canonical files

- `data/final/tcode_v4_2700.jsonl`
- `data/intermediate/t_code_dataset_v4_1600.jsonl`
- `data/intermediate/t_code_dataset_v4_2500.jsonl`
- `data/augmentation/tcode_v4_aug_200_clean_CK3703_CK3902.jsonl`
- `data/challenge/challenge_gold_100.jsonl`
- `data/challenge/challenge_filtered_99_excluding_identified_neardup.jsonl`
- `data/labels/task_token_labels_33.json`
- `data/labels/boundary_labels_3.json`

## Record fields

The final JSONL records provide the command text and aligned annotation layers used by the benchmark, including task segments, task labels, tokens, boundary labels, and task-token labels. Raw and normalized command forms are retained where available.

## Provenance and scope

The 2,500-record main resource was developed through an AI-assisted, human-curated synthetic workflow. Around 20 May 2026, GPT-5.0 was used through the ChatGPT web application to generate candidate Turkish computer-control command texts from an author-prepared prompt. Candidate texts were manually screened, revised, or rejected. All structured gold annotations, including task segmentation, task labels, tokens, boundary labels, and task-token labels, were created and verified manually. The exact original generation-prompt transcript was not retained.

The 200-record augmentation block and the 100-command challenge set were manually authored rather than generated with GPT. The `source_type` field records construction categories used during dataset development; these categories describe the design of this resource and should not be interpreted as estimates of how frequently real users issue particular types of commands.

The benchmark does not contain automatic-speech-recognition output generated by a live ASR system, application-state traces, execution-success records, or real user identity records. Some records include manually introduced ASR-like or colloquial surface variation.

## Challenge material

The 100-command challenge set was prepared separately from the 2,500-record main resource to provide a more difficult wording-shift evaluation containing phenomena such as corrections, fillers, negations, and context-sensitive expressions.

A 99-command variant excludes one near-duplicate identified during pre-augmentation review. This filename intentionally says `excluding_identified_neardup` rather than `no_neardup`: the later 200-record augmentation stage was informed by challenge-set error patterns, so the 99-command variant should not be interpreted as a fully decontaminated external test relative to the final augmented training data.

## Intended use

The resource is intended for research on command decomposition, sequence labelling, annotation analysis, and comparable benchmark evaluation. The fixed internal test and challenge material should remain outside training and future data augmentation when used for benchmark comparisons.

## Non-use and limitations

T-Code does not establish the safety or usability of an end-to-end computer-control system. Predictions should not themselves authorize destructive or consequential actions such as deleting, sending, moving, or renaming user data. Downstream systems require independent safeguards.

The repository does not provide a byte-for-byte reconstruction of every historical model-training environment. The retained final BERTurk and BiLSTM-CRF configurations are documented, while complete low-level optimizer and scheduler metadata were not retained uniformly for the mBERT and XLM-RoBERTa-base reruns.
